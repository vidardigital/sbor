name: SBOR PoX check

on:
  workflow_dispatch:

jobs:
  pox:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Compute PoX reference
        run: node scripts/pox.mjs
